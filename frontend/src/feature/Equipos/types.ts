export type Equipo = {
    id: number
    nombre: string
    categoria: string
    ubicacion: string
    estado: boolean
}

export type NewEquipo = {
    nombre: string
    categoria: string
    ubicacion: string
}